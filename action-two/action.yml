name: 'Action Two'
description: 'Second GitHub Action'
inputs:
  name:
    description: 'Your name'
    required: true
runs:
  using: 'node20'
  steps:
    - run: echo "Greetings, ${{ inputs.name }} from Action Two!"
